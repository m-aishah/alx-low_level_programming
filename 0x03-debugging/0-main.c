#include "main.h"
#include <stdio.h>

/**
 * main - tests function that print is integer is positive, negative, or zero
 * Return: Always 0 (Success)
 */

int main(void)
{
	positive_or_negative(0);
	return (0);
}
